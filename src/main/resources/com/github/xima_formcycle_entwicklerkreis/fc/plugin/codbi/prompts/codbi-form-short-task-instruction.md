You receive a partial form JSON (IPersistJson) and a natural language instruction. MODIFY the form according to the instruction and return the COMPLETE modified form JSON. Do NOT ask for more details — the user's instruction and the form data below are sufficient.
CRITICAL — EP PARAMETERS & V: EP parameters are RAW, UNQUOTED text (write { BayVIS.Ansprechpartner.Details > Salvatore Callari }, NEVER { ... > "Salvatore Callari" }). A literal person/authority NAME is NOT a variable — variable names NEVER contain spaces. The V EP takes ONLY a GLOBAL VARIABLE NAME (e.g. SALVATORE_CALLARI_CONTACT, BayVIS_Behoerde); NEVER a name with a space / a quoted person name. { BayVIS.Ansprechpartner.Details > { V > Salvatore Callari } } is WRONG — use { BayVIS.Ansprechpartner.Details > Salvatore Callari } directly. Use V only for an explicitly requested global variable that exists in the top-level "variables" array.

CRITICAL — BAYVIS BUILDING ADDRESS (NO house-number placeholder): the building EP BayVIS.Behoerden.Details.Gebaeude returns ONLY the properties bezeichnung, hausanschriftPLZ, hausanschriftOrt, hausanschriftStrasse, postanschriftPLZ, postanschriftOrt, postanschriftStrasse, logo — there is NO hausanschriftHausnummer / hausnummer property, because hausanschriftStrasse ALREADY contains street AND house number combined (e.g. "Maximilianstraße 1"). When you render a building's address as a [(property)] mapper template, NEVER emit [(hausanschriftHausnummer)] or [(hausnummer)] — it cannot be resolved. The CORRECT building-address template is exactly: "Building address: [(hausanschriftStrasse)], [(hausanschriftPLZ)] [(hausanschriftOrt)]" (the street placeholder already includes the house number).
CRITICAL — AN ADDRESS PLACEHOLDER MUST COME FROM THE BUILDING EP, NOT FROM BayVIS.Behoerden.Details: BayVIS.Behoerden.Details has NO address at all (only bezeichnungBehoerde, behoerdenart, behoerdengruppe, bezeichnung, email, id, sortierreihenfolge, logo, behoerdeZuordnungen, behoerdenGebaeudeZuordnungen) — an address template fed by it stays EMPTY. Feed [(hausanschrift*)]/[(postanschrift*)] with BayVIS.Behoerden.Details.Gebaeude using the authority ID and a building ID from BayVIS.Behoerden.Gebaeude.ID. Correct replacement for a named authority ("Amt für Digitales"): { Data.Join > { BayVIS.Behoerden.Details.Gebaeude > { I > 0 ; { BayVIS.Behoerden.ID > Amt für Digitales } } ; { I > 0 ; { BayVIS.Behoerden.Gebaeude.ID > { I > 0 ; { BayVIS.Behoerden.ID > Amt für Digitales } } } } } } ; with a global variable: { Data.Join > { BayVIS.Behoerden.Details.Gebaeude > { BayVIS.Behoerden.ID > { V > BayVIS_Behoerde } } ; { BayVIS.Behoerden.Gebaeude.ID > { BayVIS.Behoerden.ID > { V > BayVIS_Behoerde } } } } }.
CRITICAL — ONE functionality per element: data-cb-func holds EXACTLY ONE functionality. HTML.Text.Mapper (for an EP OBJECT resolved into [(property)] placeholders) and HTML.Text.Injector (for a SINGLE plain-string replacement via [[INJECTOR_REPLACEMENT]]) are MUTUALLY EXCLUSIVE — NEVER combine them on one element (data-cb-func="HTML.Text.Mapper,HTML.Text.Injector" is INVALID), never put a Mapper [(property)] template and an Injector [[INJECTOR_REPLACEMENT]] in the same element, and never add an unrelated filler EP.
CRITICAL — BayVIS.Behoerden.Details.Gebaeude NEEDS BOTH PARAMETERS: param 1 = authority ID `;` param 2 = BUILDING ID (from BayVIS.Behoerden.Gebaeude.ID). A call with ONLY the authority ID is INVALID and yields no address. Do NOT join BayVIS.Behoerden.Details (no address) into a building-address Data.Join.
WRONG: { Data.Join > { BayVIS.Behoerden.Details > { I > 0 ; { BayVIS.Behoerden.ID > Amt für Digitales } } } ; { BayVIS.Behoerden.Details.Gebaeude > { I > 0 ; { BayVIS.Behoerden.ID > Amt für Digitales } } } }
CORRECT: { Data.Join > { BayVIS.Behoerden.Details.Gebaeude > { I > 0 ; { BayVIS.Behoerden.ID > Amt für Digitales } } ; { I > 0 ; { BayVIS.Behoerden.Gebaeude.ID > { I > 0 ; { BayVIS.Behoerden.ID > Amt für Digitales } } } } } }
CRITICAL — BAYVIS PERSON named DIRECTLY (e.g. "Salvatore Callari"): pass the NAME DIRECTLY to the Details EP — "{ BayVIS.Ansprechpartner.Details > Salvatore Callari }" (no quotes). The Details EP resolves the name to the internal contact ID itself, so do NOT wrap it in "{ I > 0 ; { BayVIS.Ansprechpartner.ID > ... } }" and NEVER use the V EP with a person's literal name ("{ ... > { V > Salvatore Callari } }" is WRONG — V resolves only a form GLOBAL VARIABLE; a global variable would be "{ ... > { V > <VARIABLE_NAME> } }").
