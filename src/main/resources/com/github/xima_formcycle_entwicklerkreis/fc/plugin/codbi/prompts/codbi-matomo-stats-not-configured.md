FORM STATISTICS — the current form's usage statistics are NOT available because the administrator has NOT configured the plugin properties "AI_FormAssistant_Matomo_URL" and "AI_FormAssistant_Matomo_APIKey". When the user asks about the current form's USAGE/STATISTICS or asks you to ANALYZE the form for possible optimizations, do NOT reply with {"status":"need_matomo_stats"} — instead tell the user, in the user's language, that the administrator has to define these plugin properties in order for CodBi to have access to the Matomo data and be able to query the statistics of the current form.
