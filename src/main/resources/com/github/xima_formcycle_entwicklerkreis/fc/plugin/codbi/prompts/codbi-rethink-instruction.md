You receive a form to review for CodBi applicability. Review the form elements below and determine which CodBi functionalities apply. Return the form JSON with a _codbiApplicability field listing considered/applied/skipped items.

HTML.PANEL ON AN XCONTAINER (collapsible container — a container has NO legend): apply data-cb-func=html.panel WITH data-cb-generateheader="true" and a NON-EMPTY data-cb-autoheadertitle (the ONLY source of the container panel's header text — invent a short, meaningful title in the form's language when none is known). When the request does not describe the header's look, also generate restrained header CSS via data-cb-cssheaderunfolded (base look while unfolded), data-cb-cssheaderhover, data-cb-cssheaderactive, and a fold indicator data-cb-cssafterheadercontent / data-cb-cssafterheadercontentunfolded — every value a RAW CSS declaration list WITHOUT braces (e.g. background:#eef2f7; padding:10px 14px; border-radius:6px;). FOLDED-STATE HEADER STYLING: data-cb-cssheaderunfolded applies ONLY while the panel is unfolded (a panel that STARTS folded shows an unstyled header until unfolded) — for the folded state use data-cb-cssafterheader / data-cb-cssbeforeheader (+ their ...content), and to make the header look IDENTICAL in BOTH states ALSO emit a state-independent rule via HTML.CSS: an element with data-cb-func="HTML.CSS" and data-cb-css=".CodBi_HTML_Panel_Header { background:#eef2f7; border-radius:6px; padding:10px 14px; font-weight:600; color:#1e293b; cursor:pointer; }". Never replace or empty the existing container; keep its name/id/elements and all children. XFieldSet panels use the CodBi_HTML_Panel_* classes instead (and are already styled by the class).
