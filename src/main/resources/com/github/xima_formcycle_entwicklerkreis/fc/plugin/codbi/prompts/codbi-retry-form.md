You MUST now return the COMPLETE modified form JSON with ALL items. Do NOT return a need_codbi_details request - you already received every reference you need. Build every field/element the user asked for using the widget templates above.

CRITICAL — PRESERVE EVERY EXISTING ELEMENT: your output must contain EVERY element that is present in the input form (same `name`, `id`, `className`, `properties`, `attributes`) and keep it in its original container — unchanged — plus only the additions/modifications the user requested. NEVER omit, drop, or remove an existing element or functionality that the user did not explicitly ask to remove; an omitted existing element is lost from the published form (data loss = FAIL). Re-check your output against the input form before returning.

PANEL ON AN XCONTAINER (collapsible container): to make an existing XContainer/XContainerInvisible collapsible use data-cb-func=html.panel WITH data-cb-generateheader="true" AND a NON-EMPTY data-cb-autoheadertitle (a container has NO legend — the ONLY source of the header text; invent a short, meaningful title in the form's language if the prompt names none), and keep the container's name/id/'elements' AND all its children. When the prompt does not describe the header's look, also generate restrained header CSS via data-cb-cssheaderunfolded (base look while unfolded), data-cb-cssheaderhover, data-cb-cssheaderactive and a fold indicator data-cb-cssafterheadercontent / data-cb-cssafterheadercontentunfolded — each value a RAW CSS declaration list WITHOUT braces. FOLDED HEADERS: data-cb-cssheaderunfolded applies ONLY while unfolded (a panel that STARTS folded is unstyled) — use data-cb-cssafterheader / data-cb-cssbeforeheader (+ ...content) for the folded state and ALSO emit a state-independent rule via HTML.CSS (an element with data-cb-func="HTML.CSS" and data-cb-css=".CodBi_HTML_Panel_Header { background:#eef2f7; border-radius:6px; padding:10px 14px; font-weight:600; color:#1e293b; cursor:pointer; }") so both states look identical. XFieldSet panels use the CodBi_HTML_Panel_* classes instead.
