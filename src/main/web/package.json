{
	"private": true,
	"packageManager": "yarn@4.6.0",
	"name": "codbi",
	"version": "1.0.0",
	"stableVersion": "1.0.0",
	"author": "XIMA-formcycle-Entwicklerkreis",
	"license": "MIT",
	"description": "formcycle plugin developed as a joint cooperation of the Bavarian formcycle developer community. Adds additional features to forms and the form designer. (client side script)",
	"repository": {
		"type": "git",
		"url": "https://github.com/XIMA-formcycle-Entwicklerkreis/CodBi-Dev"
	},
	"workspaces": [
		"packages/common",
		"packages/designer",
		"packages/form",
		"packages/designer/Angular/Components/codbi-apidoc",
		"codbi-apidoc"
	],
	"scripts": {
		"docs": "yarn workspaces foreach -Avp run docs",
		"lint": "yarn workspaces foreach -Avp run lint",
		"lint-apply": "yarn workspaces foreach -Avp run lint-apply",
		"lint-apply-unsafe": "yarn workspaces foreach -Avp run lint-apply-unsafe",
		"test": "yarn workspaces foreach -Avp run test",
		"build": "yarn workspaces foreach -Avt run build",
		"build-dev": "yarn workspaces foreach -Avt run build-dev"
	},
	"devDependencies": {
		"@biomejs/biome": "1.9.4",
		"@types/node": "~22.9.0",
		"typescript": "~5.7.3"
	},
	"resolutions": {
		"canvas@npm:^2.11.2": "patch:canvas@npm%3A2.11.2#~/.yarn/patches/canvas-npm-2.11.2-824d893a31.patch"
	}
}
