/**
 * Previously provided the CodBi AI Workflow Assistant popup dialog (ALT+W hotkey).
 * Replaced by {@link enableAICodBiAssistantDialog} (ALT+A unified assistant).
 */
export function enableAIWorkflowAssistantDialog(): void {
  // Replaced by enableAICodBiAssistantDialog (ALT+A unified assistant).
}
