import { a as s } from "./chunk-K3A632J4.js";
import { a as h } from "./chunk-QM2ZX7FA.js";
import { a as c } from "./chunk-W23DHSE2.js";
import { a as p } from "./chunk-MUWAMKOD.js";
import { f, g as l, h as m } from "./chunk-RS4WWU7K.js";
var D = f(c(), 1);
var t = class t {
  static functionality(e, i) {
    Array.isArray(e.minimum) && (e.minimum = e.minimum[0]),
      Array.isArray(e.msghigher) && (e.msghigher = e.msghigher[0]),
      Array.isArray(e.delimiter) && (e.delimiter = e.delimiter[0]),
      Array.isArray(e.reverse) && (e.reverse = e.reverse[0]),
      Array.isArray(e.unit) && (e.unit = e.unit[0]);
    let n = (0, D.getJQuery)();
    n(i).data("datepicker") === 1 && n(i).datepicker(),
      (e.reverse = e.reverse === void 0 ? !1 : s.tsCheck(e.reverse, "string").toLowerCase() === "true"),
      (e.unit = e.unit === void 0 ? "y" : s.tsCheck(e.unit, "string").substring(0, 1).toLowerCase()),
      (e.unit = ["d", "w", "m", "y"].includes(e.unit) ? e.unit : "y"),
      n(i).datepicker(
        "option",
        e.reverse ? { minDate: `+${e.minimum}${e.unit}` } : { maxDate: `-${e.minimum}${e.unit}` },
      );
    let r = new Date();
    switch (e.unit) {
      case "d":
        r.setDate(
          e.reverse
            ? new Date().getDate() + Number.parseInt(e.minimum)
            : new Date().getDate() - Number.parseInt(e.minimum),
        );
        break;
      case "w":
        r.setDate(
          e.reverse
            ? new Date().getDate() + Number.parseInt(e.minimum) * 7
            : new Date().getDate() - Number.parseInt(e.minimum) * 7,
        );
        break;
      case "m":
        r.setMonth(
          e.reverse
            ? new Date().getMonth() + Number.parseInt(e.minimum)
            : new Date().getMonth() - Number.parseInt(e.minimum),
        );
        break;
      case "y":
        r.setFullYear(
          e.reverse
            ? new Date().getFullYear() + Number.parseInt(e.minimum)
            : new Date().getFullYear() - Number.parseInt(e.minimum),
        );
        break;
      default:
        r.setFullYear(
          e.reverse
            ? new Date().getFullYear() + Number.parseInt(e.minimum)
            : new Date().getFullYear() - Number.parseInt(e.minimum),
        );
    }
    n(i).on("change", (b) => {
      e.reverse
        ? r.setHours(0, 0, 0, 0) >
          new Date(
            i.value
              .split(e.delimiter && typeof e.delimiter == "string" ? e.delimiter : ".")
              .reduce((u, g, a) => g + (a === 0 ? "" : "/") + u),
          ).setHours(0, 0, 0, 0)
          ? n(i).error(
              e.msghigher && typeof e.msghigher == "string"
                ? e.msghigher.replace(/\[%ERROR_DATE%\]/g, r.toLocaleDateString())
                : `Date has to be later or same as ${r.toLocaleDateString()}.`,
            )
          : n(i).error("")
        : r.setHours(0, 0, 0, 0) <
            new Date(
              i.value
                .split(e.delimiter && typeof e.delimiter == "string" ? e.delimiter : ".")
                .reduce((u, g, a) => g + (a === 0 ? "" : "/") + u),
            ).setHours(0, 0, 0, 0)
          ? n(i).error(
              e.msghigher && typeof e.msghigher == "string"
                ? e.msghigher.replace(/\[%ERROR_DATE%\]/g, r.toLocaleDateString())
                : `Date has to be earlier or same as ${r.toLocaleDateString()}.`,
            )
          : n(i).error("");
    });
  }
};
(t.registered = window.codbi.registerFunctionality("Date.Min", t.functionality)),
  l(
    [
      p.ParamvalueProvider,
      m(0, s.PRE("string", "minimum")),
      m(0, s.PRE("string", "msghigher")),
      m(1, h.PRE("INPUT", !1, "tagName")),
    ],
    t,
    "functionality",
    1,
  );
var y = t;
export { y as Date_Min };
