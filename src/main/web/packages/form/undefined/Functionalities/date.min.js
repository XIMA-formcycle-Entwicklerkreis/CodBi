import { a as E } from "./chunk-PSEWTT4Z.js";
import { a } from "./chunk-M2SNI3IN.js";
import { a as f } from "./chunk-4JLAI42Q.js";
import { a as b } from "./chunk-KEJSWGMR.js";
import { a as m } from "./chunk-SEUS6MHP.js";
import { a as s } from "./chunk-CDLTIEKC.js";
import { f as v, g as D, h as t, p as w } from "./chunk-UTJJRBTX.js";
var h = v(f(), 1);
var u = class {
  static functionality(e, n) {
    let i = (0, h.getJQuery)();
    i(n).data("datepicker") === 1 && i(n).datepicker(),
      (e.reverse = e.reverse ? (typeof e.reverse == "boolean" ? e.reverse : e.reverse.toLowerCase() === "true") : !1),
      (e.unit = ["d", "w", "m", "y"].includes(e.unit) ? e.unit : "y"),
      i(n).datepicker(
        "option",
        e.reverse ? { minDate: `+${e.minimum}${e.unit}` } : { maxDate: `-${e.minimum}${e.unit}` },
      );
    let r = new Date();
    switch (e.unit) {
      case "d":
        r.setDate(
          e.reverse
            ? new Date().getDate() + Number.parseInt(e.minimum)
            : new Date().getDate() - Number.parseInt(e.minimum),
        );
        break;
      case "w":
        r.setDate(
          e.reverse
            ? new Date().getDate() + Number.parseInt(e.minimum) * 7
            : new Date().getDate() - Number.parseInt(e.minimum) * 7,
        );
        break;
      case "m":
        r.setMonth(
          e.reverse
            ? new Date().getMonth() + Number.parseInt(e.minimum)
            : new Date().getMonth() - Number.parseInt(e.minimum),
        );
        break;
      case "y":
        r.setFullYear(
          e.reverse
            ? new Date().getFullYear() + Number.parseInt(e.minimum)
            : new Date().getFullYear() - Number.parseInt(e.minimum),
        );
        break;
      default:
        r.setFullYear(
          e.reverse
            ? new Date().getFullYear() + Number.parseInt(e.minimum)
            : new Date().getFullYear() - Number.parseInt(e.minimum),
        );
    }
    i(n).on("change", (I) => {
      e.reverse
        ? r.setHours(0, 0, 0, 0) >
          new Date(n.value.split(e.delimiter || ".").reduce((g, l, p) => l + (p === 0 ? "" : "/") + g)).setHours(
            0,
            0,
            0,
            0,
          )
          ? i(n).error(
              e.msghigher
                ? e.msghigher.replace(/\[%ERROR_DATE%\]/g, r.toLocaleDateString())
                : `Date has to be later or same as ${r.toLocaleDateString()}.`,
            )
          : i(n).error("")
        : r.setHours(0, 0, 0, 0) <
            new Date(n.value.split(e.delimiter || ".").reduce((g, l, p) => l + (p === 0 ? "" : "/") + g)).setHours(
              0,
              0,
              0,
              0,
            )
          ? i(n).error(
              e.msghigher
                ? e.msghigher.replace(/\[%ERROR_DATE%\]/g, r.toLocaleDateString())
                : `Date has to be earlier or same as ${r.toLocaleDateString()}.`,
            )
          : i(n).error("");
    });
  }
};
D(
  [
    w.ParamvalueProvider,
    t(0, s.PRE("string", "msghigher :: delimiter :: unit")),
    t(0, a.PRE(new s("string"), new m(/^\d+$/), "minimum")),
    t(0, a.PRE(new s("string"), new s("number"), "minimum", !0)),
    t(0, a.PRE(new s("string"), new m(m.stdExp.boolean), "reverse")),
    t(0, a.PRE(new s("string"), new s("boolean"), "reverse", !0)),
    t(0, m.PRE(/(D|W|M|Y)/i, "unit", "Is the Unit-Parameter not one of the allowed values (D, W, M or Y)?")),
    t(
      1,
      b.PRE(HTMLInputElement, void 0, 'Is it not an <input type = "text"/> that is tagged with this functionality?'),
    ),
    t(1, E.PRE("text", !1, "type")),
  ],
  u,
  "functionality",
  1,
);
window.codbi.registerFunctionality("Date.Min", u.functionality.bind(u));
export { u as Date_Min };
